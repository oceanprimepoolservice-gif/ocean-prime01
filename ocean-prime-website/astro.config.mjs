import { defineConfig } from 'astro/config';

// Update `site` to the production domain before deploying.
export default defineConfig({
  site: 'https://oceanprimepoolservice.com',
  trailingSlash: 'always',
});
